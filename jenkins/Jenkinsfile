pipeline {
    agent {
        docker {
            image 'node:8-alpine'
            args '-p 3000:3000'
        }
    }
    environment {
        HOME = '.'
        CI = 'true'
        NODE_PATH = './src'
    }
    stages {
        stage('Install') {
            steps {
                sh 'npm install'
            }
        }
        stage('ESlint') {
            steps {
                sh 'npm run lint'
            }
        }
        stage('Deploy') {
            steps {
                sh 'ls'
                sh 'cd build'
                sh 'ls'
                sh 'printenv'
                sh 'npm run deploy'
            }
        }
    }
}